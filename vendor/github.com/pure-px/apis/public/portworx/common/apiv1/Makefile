PROTO_FILES = pagination.proto \
              secrets.proto \
              meta.proto \
              sort.proto \
              platformresource.proto \
              selector.proto \
              revision.proto \
              healthcheck.proto

all: $(PROTO_FILES)

include $(TOP)/make/common.mk
