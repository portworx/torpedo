package tests

import (
	"context"
	"fmt"
	"strings"
	"sync"
	"time"

	. "github.com/onsi/ginkgo/v2"
	"github.com/pborman/uuid"
	api "github.com/portworx/px-backup-api/pkg/apis/v1"
	"github.com/pure-px/torpedo/drivers/backup"
	"github.com/pure-px/torpedo/drivers/scheduler"
	"github.com/pure-px/torpedo/pkg/log"
	. "github.com/pure-px/torpedo/tests"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/status"
)

type user struct {
	name string
	ctx  context.Context
}

var _ = Describe("{ClusterShareWithRestoreGroupAndMultipleUserTestcases}", Label(TestCaseLabelsMap[ClusterShare]...), func() {
	var (
		scheduledAppContexts []*scheduler.Context
		bkpNamespaces        []string
		providers            []string            = GetBackupProviders()
		infraAdminRole       backup.PxBackupRole = backup.InfrastructureOwner
		superAdminRole       backup.PxBackupRole = backup.SuperAdmin
		cloudCredName        string
		cloudCredUID         string
		backupLocationUID    string
		bkpLocationName      string
		backupLocationMap    map[string]string
		pxbUsers             map[int]user
	)
	JustBeforeEach(func() {
		backupLocationMap = make(map[string]string)
		scheduledAppContexts = make([]*scheduler.Context, 0)
		bkpNamespaces = make([]string, 0)
		cloudCredName, cloudCredUID, backupLocationUID, bkpLocationName = "", "", "", ""

		// Schedule an Application
		appContexts := ScheduleApplications(TaskNamePrefix)
		for _, ctx := range appContexts {
			ctx.ReadinessTimeout = AppReadinessTimeout
			namespace := GetAppNamespace(ctx, TaskNamePrefix)
			bkpNamespaces = append(bkpNamespaces, namespace)
			scheduledAppContexts = append(scheduledAppContexts, ctx)
		}

		// Validate the application
		ValidateApplications(scheduledAppContexts)

		// Create two test users with infra admin role
		var err error
		numberOfUsers := 2
		log.InfoD("Creating %d users", numberOfUsers)
		testusers := CreateUsers(numberOfUsers)
		pxbUsers = make(map[int]user)

		adminCtx, err := backup.GetAdminCtxFromSecret()
		log.FailOnError(err, "Fetching admin user ctx")

		pxbUsers[0] = user{name: "ADMIN USER", ctx: adminCtx}
		//pxbUsers = map[int]user{1: {name: "testuser1"}, 2: {name: "testuser2"}}
		for index, testuser := range testusers {
			err = backup.AddRoleToUser(testuser, infraAdminRole, fmt.Sprintf("Adding %v role to %s", infraAdminRole, testuser))
			log.FailOnError(err, "Failed to add role %s to the user %s", infraAdminRole, testuser)

			ctxNonAdmin, err := backup.GetNonAdminCtx(testuser, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[index+1] = user{testuser, ctxNonAdmin}
		}

		// Create backup location and cloud setting with admin user
		log.InfoD("Creating backup location and cloud setting with admin user")
		for _, provider := range providers {
			cloudCredName = fmt.Sprintf("%s-%s-%v", "cred", provider, time.Now().Unix())
			bkpLocationName = fmt.Sprintf("autogenerated-backup-location-%v", time.Now().Unix())
			cloudCredUID = uuid.New()
			backupLocationUID = uuid.New()
			backupLocationMap[backupLocationUID] = bkpLocationName
			err := CreateCloudCredential(provider, cloudCredName, cloudCredUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of cloud credential named [%s] for org [%s] with [%s] as provider", cloudCredName, BackupOrgID, provider))
			err = CreateBackupLocationWithContext(provider, bkpLocationName, backupLocationUID, cloudCredName, cloudCredUID, getGlobalBucketName(provider), BackupOrgID, "", pxbUsers[0].ctx, true)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creating backup location %s", bkpLocationName))
		}

		log.InfoD("Sharing backup location with user1")
		err = AddBackupLocationOwnership(bkpLocationName, backupLocationUID, []string{pxbUsers[1].name}, nil, Read, Invalid, pxbUsers[0].ctx)
		log.FailOnError(err, "failed to update backup location [%v] ownership", bkpLocationName)

		// Share the Backup Location with testUser2
		log.InfoD("Sharing backup location with user2")
		err = AddBackupLocationOwnership(bkpLocationName, backupLocationUID, []string{pxbUsers[2].name}, nil, Read, Invalid, pxbUsers[0].ctx)
		log.FailOnError(err, "failed to update backup location [%v] ownership", bkpLocationName)
	})
	// This testcase verifies whether the restores created/owned by the user were deleted during the cluster un-share.
	It("VerifyRestoreObjectsAreDeletedCreatedByNonSuperAdmin", func() {
		StartPxBackupTorpedoTest("VerifyRestoreObjectsAreDeletedCreatedByNonSuperAdmin", "VerifyRestoreObjectsAreDeletedCreatedByNonSuperAdmin during the cluster unshare", nil, 301039, Sgajawada, Q2FY25)

		var (
			backupName       = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			restoreName      = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping = make(map[string]string)
			clusterUid       string
			restoreUID       string
		)

		Step("Create a cluster object with User1 and Share with User2", func() {
			err := AddSourceCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, pxbUsers[1].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Create a backup and restore it on the same cluster(Shared Cluster) with user(User2)", func() {
			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
			restoreUID, err = Inst().Backup.GetRestoreUID(pxbUsers[2].ctx, restoreName, BackupOrgID)
			log.FailOnError(err, "failed to get restore %s uid", restoreName)

		})

		Step("User1 UnShares the cluster from User2", func() {
			// Un-Share the source cluster with testuser2
			_, err := UnShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Validate the restore object", func() {
			log.InfoD("validating the restore object")
			restoreInspectRequest := &api.RestoreInspectRequest{
				Name:  restoreName,
				OrgId: BackupOrgID,
				Uid:   restoreUID,
			}
			_, err := Inst().Backup.InspectRestore(pxbUsers[2].ctx, restoreInspectRequest)
			log.FailOnNoError(err, "inspect restore %s", restoreName)
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})
	})
	// This testcase verifies whether the restores created/owned by the user with super-admin role were not deleted during the cluster un-share.
	It("VerifyRestoreObjectsAreNotDeletedCreatedBySuperAdmin", func() {
		StartPxBackupTorpedoTest("VerifyRestoreObjectsAreNotDeletedCreatedBySuperAdmin", "VerifyRestoreObjectsAreNotDeletedCreatedBySuperAdmin during the cluster unshare", nil, 301041, Sgajawada, Q2FY25)

		var (
			backupName       = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			restoreName      = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping = make(map[string]string)
			clusterUid       string
			restoreUID       string
		)

		Step("Update testuser2 with super admin role", func() {
			err := backup.AddRoleToUser(pxbUsers[2].name, superAdminRole, fmt.Sprintf("Adding %v role to %s", infraAdminRole, pxbUsers[2].name))
			log.FailOnError(err, "failed to add role %s to the user %s", superAdminRole, pxbUsers[2].name)

			ctxNonAdmin, err := backup.GetNonAdminCtx(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			user := pxbUsers[2]
			user.ctx = ctxNonAdmin
			pxbUsers[2] = user
		})

		Step("Create a cluster object with User1 and Share with User2", func() {
			err := AddSourceCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, pxbUsers[1].name))
			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Create a backup and restore it on the same cluster(Shared Cluster) with user(User2)", func() {
			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
			restoreUID, err = Inst().Backup.GetRestoreUID(pxbUsers[2].ctx, restoreName, BackupOrgID)
			log.FailOnError(err, "failed to get restore %s uid", restoreName)
		})

		Step("User1 UnShares the cluster from User2", func() {
			// Un-Share the source cluster with testuser2
			_, err := UnShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Validate the restore object", func() {
			log.InfoD("validating the restore object")
			restoreInspectRequest := &api.RestoreInspectRequest{
				Name:  restoreName,
				OrgId: BackupOrgID,
				Uid:   restoreUID,
			}
			_, err := Inst().Backup.InspectRestore(pxbUsers[2].ctx, restoreInspectRequest)
			log.FailOnError(err, "inspect restore %s", restoreName)
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			log.InfoD(fmt.Sprintf("Delete restore with name [%s]", restoreName))
			err = DeleteRestore(restoreName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting restore [%s]", restoreName))

			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})

	})
	// This testcase verifies the cluster un-share operation when there is a backupschedule.
	It("VerifyClusterUnShareWhenBackupSchedulesAreDeleted", func() {
		StartPxBackupTorpedoTest("VerifyClusterUnShareWhenBackupSchedulesAreDeleted", "VerifyClusterUnShareWhenBackupSchedulesAreDeleted during the cluster unshare", nil, 301040, Sgajawada, Q2FY25)

		var (
			scheduleName               string
			periodicSchedulePolicyName string
			periodicSchedulePolicyUid  string
			clusterUid                 string
			allScheduleBackupNames     []string
		)

		Step("Create a cluster object with User1 and Share with User2", func() {
			err := AddSourceCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, pxbUsers[1].name))
			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Create a backup schedule with User2", func() {
			// Create Schedule Policy
			periodicSchedulePolicyName = fmt.Sprintf("%s-%s", "periodic", RandomString(5))
			periodicSchedulePolicyUid = uuid.New()
			periodicSchedulePolicyInterval := int64(15)
			err := CreateBackupScheduleIntervalPolicy(5, periodicSchedulePolicyInterval, 5, periodicSchedulePolicyName, periodicSchedulePolicyUid, BackupOrgID, pxbUsers[2].ctx, false, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of periodic schedule policy of interval [%v] minutes named [%s] ", periodicSchedulePolicyInterval, periodicSchedulePolicyName))

			// Take Backup Schedule
			log.InfoD("Taking schedule backup of multiple namespaces")
			scheduleName = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			_, err = CreateScheduleBackupWithoutCheck(scheduleName, SourceClusterName, clusterUid, bkpLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of scheduled backup with schedule name [%s]", scheduleName))
		})

		Step("User1 UnShares the cluster from User2", func() {
			// UnShare the source cluster to testuser2
			_, err := UnShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			log.InfoD("UnshareCluster error response: %v", err)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Delete the backup schedule and unshare the cluster", func() {
			// TODO: Here need to delete the backup schedule with super-admin role user
			log.InfoD("Deleting backup schedule [%s]", scheduleName)
			scheduleUID, err := GetScheduleUID(scheduleName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backupschedule uid", scheduleName))

			allScheduleBackupNames, err = Inst().Backup.GetAllScheduleBackupNames(pxbUsers[2].ctx, scheduleName, BackupOrgID)
			log.FailOnError(err, "failed to get all schedule backup names with schedule name %s of the user %s", scheduleName, pxbUsers[2].name)

			err = DeleteScheduleWithUIDAndWait(scheduleName, scheduleUID, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of backup schedule [%s]", scheduleName))

			// UnShare the source cluster to testuser2
			_, err = UnShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s user using %s ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Cleanup", func() {
			log.InfoD("allScheduleBackupNames: %v", allScheduleBackupNames)
			for i := len(allScheduleBackupNames) - 1; i >= 0; i-- {
				backupName := allScheduleBackupNames[i]
				backupUid, err := Inst().Backup.GetBackupUID(pxbUsers[2].ctx, backupName, BackupOrgID)
				if err != nil && !strings.Contains(err.Error(), "not found") {
					log.FailOnError(err, "failed to fetch backup %s uid of the user %s", backupName, pxbUsers[2].name)
				}
				if backupUid != "" {
					err = DeleteBackupAndWaitForCompletion(backupName, backupUid, BackupOrgID, pxbUsers[2].ctx)
					log.FailOnError(err, "failed to delete schedule backup %s of the user %s", backupName, pxbUsers[2].name)
				}
			}

			log.InfoD("Deleting schedule policy [%s]", periodicSchedulePolicyName)
			schedulePolicyDeleteRequest := &api.SchedulePolicyDeleteRequest{
				Name:  periodicSchedulePolicyName,
				Uid:   periodicSchedulePolicyUid,
				OrgId: BackupOrgID,
			}
			_, err := Inst().Backup.DeleteSchedulePolicy(pxbUsers[2].ctx, schedulePolicyDeleteRequest)
			log.FailOnError(err, "failed to delete schedule policy %s of the user %s", periodicSchedulePolicyName, pxbUsers[2].name)

			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})

	})
	// This testcase verifies the restore creation on the shared cluster by the shared user.
	It("VerifyRestoreCreateBySharedClusterUser", func() {
		StartPxBackupTorpedoTest("VerifyRestoreCreateBySharedClusterUser", "VerifyRestoreCreateBySharedClusterUser during the cluster unshare", nil, 301042, Sgajawada, Q2FY25)

		var (
			backupName            = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			restoreName           = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping      = make(map[string]string)
			clusterUid            string
			destinationClusterUid string
		)

		Step("Create a cluster object(destination cluster) with User1 and Share with User2", func() {
			err := AddDestinationCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of [%s] cluster with %s ctx", DestinationClusterName, pxbUsers[1].name))
			destinationClusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, DestinationClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Share the destination cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, DestinationClusterName, destinationClusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of [%s] cluster with %s user using %s ctx", DestinationClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})

		Step("Create another cluster object(Source Cluster) with User2", func() {
			err := AddSourceCluster(pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of [%s] cluster with %s ctx", SourceClusterName, pxbUsers[2].name))
			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[2].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("create a backup from soure and restore it on destination(Shared Cluster) with user2", func() {
			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName, namespaceMapping, DestinationClusterName, destinationClusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			err = DeleteRestore(restoreName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting restore [%s]", restoreName))

			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))

			err = DeleteClusterWithUID(DestinationClusterName, destinationClusterUid, BackupOrgID, pxbUsers[1].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", DestinationClusterName))
		})
	})
	// This test case verifies that after sharing a cluster with a new user, the user can access the cluster, create a backup, and after the cluster is revoked by the admin, confirm that the cluster is no longer visible to the user, while ensuring the backup remains accessible and can be deleted.
	It("VerifyClusterShareAccessAndBackupCreationWithRevocation", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareAccessAndBackupCreationWithRevocation", "VerifyClusterShareAccessAndBackupCreationWithRevocation", nil, 301035, Pamathur, Q2FY25)

		var (
			backupName2  = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
			backupName   = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			backupDriver = Inst().Backup
			clusterUid   string
		)
		Step("Create a cluster & backup object from admin and Share with User1", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[0].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] from user [%s]", backupName, pxbUsers[0].name))

			// Share the source cluster to testuser1
			_, err = ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[1].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[1].name, pxbUsers[0].name))
		})
		Step("Create a backup object using shared cluster from user1", func() {
			backupResp, err := Inst().Backup.EnumerateBackup(pxbUsers[1].ctx, &api.BackupEnumerateRequest{
				OrgId: BackupOrgID,
			})
			dash.VerifyFatal(len(backupResp.GetBackups()), 0, fmt.Sprintf("Enumerating backups for org [%s] using [%s]", BackupOrgID, pxbUsers[1].ctx))

			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[1].name))
		})
		Step("Delete Cluster using admin and verify backups", func() {
			err := DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s] using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			_, err = backupDriver.InspectBackup(pxbUsers[0].ctx, &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  backupName2,
			})
			dash.VerifyFatal(err, nil, fmt.Sprintf("Inspection of backup [%s] using [%s] ctx", backupName2, pxbUsers[0].name))
		})
		Step("Verify Backup from User1 that was created from unshared cluster", func() {
			_, err := backupDriver.InspectCluster(pxbUsers[1].ctx, &api.ClusterInspectRequest{
				OrgId: BackupOrgID,
				Name:  SourceClusterName,
			})
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Inspection of cluster [%s] using [%s] ctx", SourceClusterName, pxbUsers[1].name))

			_, err = backupDriver.InspectBackup(pxbUsers[1].ctx, &api.BackupInspectRequest{
				Name:  backupName2,
				OrgId: BackupOrgID,
			})
			dash.VerifyFatal(err, nil, fmt.Sprintf("Inspection of backup [%s] using [%s] ctx", backupName2, pxbUsers[1].name))
		})
	})
	// This test case verifies the behavior of cluster sharing and backup creation between two users. It ensures that after a super admin revokes the shared cluster from testUser2, testUser1 can still create backups, testUser2 is prevented from creating new backups, and testUser2 can delete the backup created prior to the revocation.
	It("VerifyClusterShareRevocationBackupAccessAndDeletionBehavior", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareRevocationBackupAccessAndDeletionBehavior", "VerifyClusterShareRevocationBackupAccessAndDeletionBehavior", nil, 301036, Pamathur, Q2FY25)
		var (
			backupName2 = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
			backupName  = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			clusterUid  string
		)
		Step("Create a cluster & backup object with User1 and Share with User2", func() {
			err := AddSourceCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[1].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[1].name))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})
		// 6. Login as TestUser2 and create Backup on shared cluster.
		Step("Create a backup on the same cluster(Shared Cluster) with user(User2)", func() {
			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[2].name))
		})
		Step("Revoke the cluster share from User2", func() {
			_, err := UnShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[0].name))
		})
		Step("Create Backup From Both Users and Validate", func() {
			// Create Backup 2 from user1
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[1].name))

			// Create Backup 2 from user 2
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[2].name))
		})
	})
	// This test case verifies that after sharing a cluster and a backup with full access to testUser1, the user can successfully restore from the shared backup, and subsequently delete both the backup created by the admin and their own backup. Each action is validated to ensure the correct permissions and access control are enforced.
	It("VerifyClusterShareBackupRestoreAndDeletionBySharedUserWithFullAccess", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareBackupRestoreAndDeletionBySharedUserWithFullAccess", "VerifyClusterShareBackupRestoreAndDeletionBySharedUserWithFullAccess", nil, 301037, Pamathur, Q2FY25)
		var (
			backupName       = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			backupName2      = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
			restoreName      = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping = make(map[string]string)
			clusterUid       string
		)
		Step("Create a cluster & backup object with super amin and Share with User1", func() {

			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", BackupNamePrefix, pxbUsers[0].name))

			// Share the source cluster to testuser1
			_, err = ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[1].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[1].name, pxbUsers[0].name))
		})
		Step("Create a backup object using shared cluster from user1", func() {
			resp, err := Inst().Backup.EnumerateBackup(pxbUsers[1].ctx, &api.BackupEnumerateRequest{
				OrgId: BackupOrgID,
			})
			dash.VerifyFatal(err, nil, "Enumerating backups")
			dash.VerifyFatal(len(resp.GetBackups()), 0, fmt.Sprintf("Enumerating backups using [%s] ctx", pxbUsers[1].name))

			// Take Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[1].name))
		})
		Step("Share the backup with User1", func() {
			err := ShareBackup(backupName, nil, []string{pxbUsers[1].name}, FullAccess, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of backup [%s] with %s user using [%s] ctx", backupName, pxbUsers[1].name, pxbUsers[0].name))
		})
		Step("Restore the backup and delete the backups", func() {
			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err := CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s] using [%s] ctx", restoreName, pxbUsers[1].name))
		})
	})
	// This test case verifies that after testUser1 shares a cluster with testUser2, testUser2 can create a backup schedule on the shared cluster. It also confirms that when testUser1 attempts to revoke the cluster share from testUser2, the operation should fail, ensuring proper handling of permissions during active backup schedules.
	It("VerifyBackupScheduleCreationAndFailedClusterShareRevocation", func() {
		StartPxBackupTorpedoTest("VerifyBackupScheduleCreationAndFailedClusterShareRevocation", "VerifyBackupScheduleCreationAndFailedClusterShareRevocation", nil, 301038, Pamathur, Q2FY25)
		var (
			periodicSchedulePolicyName     = fmt.Sprintf("%s-%s", "periodic", RandomString(5))
			periodicSchedulePolicyUid      = uuid.New()
			periodicSchedulePolicyInterval = int64(15)
			scheduleName                   = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			clusterUid                     string
		)
		Step("Create a cluster object with User1 and share it to user2", func() {
			err := AddSourceCluster(pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[1].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[1].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[1].name))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))
		})
		Step("Create a backup schedule on the shared cluster with User2", func() {
			// Create Schedule Policy
			err := CreateBackupScheduleIntervalPolicy(5, periodicSchedulePolicyInterval, 5, periodicSchedulePolicyName, periodicSchedulePolicyUid, BackupOrgID, pxbUsers[2].ctx, false, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of periodic schedule policy of interval [%v] minutes named [%s] using [%s] ctx", periodicSchedulePolicyInterval, periodicSchedulePolicyName, pxbUsers[2].name))

			// Take Backup Schedule
			log.InfoD("Taking schedule backup of multiple namespaces")
			_, err = CreateScheduleBackupWithoutCheck(scheduleName, SourceClusterName, clusterUid, bkpLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of scheduled backup with schedule name [%s] using [%s] ctx", scheduleName, pxbUsers[2].name))
		})
		Step("Revoke the cluster share from User2", func() {
			_, err := UnShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Verifying Unshare of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[1].name))

			err = SuspendAndDeleteSchedule(scheduleName, periodicSchedulePolicyName, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of schedule [%s] using [%s] ctx", scheduleName, pxbUsers[2].name))
		})
	})
	// This test case verifies that after sharing a cluster with a group containing multiple users, both users (user1 and user2) can successfully create backups on the shared cluster. It ensures that group-level access permissions are properly applied for backup creation.
	It("VerifyClusterShareWithGroupAndSuccessfulBackupCreationByAllGroupMembers", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareWithGroupAndSuccessfulBackupCreationByAllGroupMembers", "VerifyClusterShareWithGroupAndSuccessfulBackupCreationByAllGroupMembers", nil, 301043, Pamathur, Q2FY25)
		var (
			groupName   = fmt.Sprintf("%s-%s", "group", RandomString(5))
			backupName  = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			backupName2 = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
			clusterUid  string
		)
		Step("Create a Group from admin and assign this to user1 and user2", func() {
			// Create Group
			err := backup.AddGroup(groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of group [%s] using [%s] ctx", groupName, pxbUsers[0].name))

			err = backup.AddRoleToGroup(groupName, backup.ApplicationOwner, "testing from torpedo")
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of role [%s] to group [%s] using [%s] ctx", backup.ApplicationOwner, groupName, pxbUsers[0].name))

			// Add User to Group
			err = backup.AddGroupToUser(pxbUsers[1].name, groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of user [%s] to group [%s] using [%s] ctx", pxbUsers[1].name, groupName, pxbUsers[0].name))

			err = backup.AddGroupToUser(pxbUsers[2].name, groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of user [%s] to group [%s] using [%s] ctx", pxbUsers[2].name, groupName, pxbUsers[0].name))

			err = AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[0].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			// Share the source cluster to group
			_, err = ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, nil, []string{groupName}, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] group using [%s] ctx", SourceClusterName, groupName, pxbUsers[0].name))

		})
		Step("Create a backup on the shared cluster with User1 & User2", func() {

			ctxNonAdmin, err := backup.GetNonAdminCtx(pxbUsers[1].name, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[1] = user{name: pxbUsers[1].name, ctx: ctxNonAdmin}

			ctxNonAdmin, err = backup.GetNonAdminCtx(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[2] = user{name: pxbUsers[2].name, ctx: ctxNonAdmin}
			// Create Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[1].name))

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[2].name))
		})

		Step("Cleanup", func() {
			err := backup.DeleteGroup(groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of group [%s] using [%s] ctx", groupName, pxbUsers[0].name))
		})

	})
	// This test case verifies that after sharing a cluster with a group, both user1 and user2 can successfully create backups and restores from the shared cluster. After the cluster is unshared from the group by the admin, it confirms that no restore objects are listed for the cluster, ensuring proper revocation of access.
	It("VerifyBackupRestoreByGroupAndNoRestoreAccessAfterClusterUnshare", func() {
		StartPxBackupTorpedoTest("VerifyBackupRestoreByGroupAndNoRestoreAccessAfterClusterUnshare", "VerifyBackupRestoreByGroupAndNoRestoreAccessAfterClusterUnshare", nil, 301044, Pamathur, Q2FY25)
		var (
			restoreName      = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping = make(map[string]string)
			clusterUid       string
		)
		var (
			groupName   = fmt.Sprintf("%s-%s", "group", RandomString(5))
			backupName  = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			backupName2 = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
		)
		Step("Create a Group from admin and assign this to user1 and user2", func() {
			// Create Group
			err := backup.AddGroup(groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of group [%s] using [%s] ctx", groupName, pxbUsers[0].name))

			err = backup.AddRoleToGroup(groupName, backup.ApplicationOwner, "testing from torpedo")
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of role [%s] to group [%s] using [%s] ctx", backup.ApplicationOwner, groupName, pxbUsers[0].name))

			// Add User to Group
			err = backup.AddGroupToUser(pxbUsers[1].name, groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of user [%s] to group [%s] using [%s] ctx", pxbUsers[1].name, groupName, pxbUsers[0].name))

			err = backup.AddGroupToUser(pxbUsers[2].name, groupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying addition of user [%s] to group [%s] using [%s] ctx", pxbUsers[2].name, groupName, pxbUsers[0].name))

			err = AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[0].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			// Share the source cluster to group
			_, err = ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, nil, []string{groupName}, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] group using [%s] ctx", SourceClusterName, groupName, pxbUsers[0].name))

		})
		Step("Create a backup on the shared cluster with User1", func() {
			ctxNonAdmin, err := backup.GetNonAdminCtx(pxbUsers[1].name, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[1] = user{name: pxbUsers[1].name, ctx: ctxNonAdmin}

			ctxNonAdmin, err = backup.GetNonAdminCtx(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[2] = user{name: pxbUsers[2].name, ctx: ctxNonAdmin}
			// Create Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[1].name))

			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s] using [%s] ctx", restoreName, pxbUsers[1].name))

		})

		Step("Create a backup on the shared cluster with User2", func() {
			// Create Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[2].name))

			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName2, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s] using [%s] ctx", restoreName, pxbUsers[2].name))

		})

		Step("Unshare the cluster from the group", func() {
			_, err := UnShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, nil, []string{groupName})
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with [%s] group using [%s] ctx", SourceClusterName, groupName, pxbUsers[0].name))

			// check after un sharing of cluster from groups , restore should get deleted automatically
			resp, err := Inst().Backup.EnumerateRestore(pxbUsers[0].ctx, &api.RestoreEnumerateRequest{
				OrgId: BackupOrgID,
			})
			dash.VerifyFatal(err, nil, fmt.Sprintf("Error while restore enumerate for the user %s: %v", pxbUsers[0].name, err))
			dash.VerifyFatal(len(resp.Restores), 0, fmt.Sprintf("Verifying restore list is empty after unshare of source [%s] cluster with [%s] group using [%s] ctx", SourceClusterName, groupName, pxbUsers[0].name))
		})
	})
	// This test case verifies that after sharing a cluster and its backups from User1 to User2, User2 can successfully create a backup on the shared cluster and perform a restore from the backup originally created by User1. This ensures proper functionality of cluster and backup sharing with restoration capabilities.
	It("VerifyClusterAndBackupShareWithBackupCreationAndRestoreBySharedUser", func() {
		StartPxBackupTorpedoTest("VerifyClusterAndBackupShareWithBackupCreationAndRestoreBySharedUser", "VerifyClusterAndBackupShareWithBackupCreationAndRestoreBySharedUser", nil, 301045, Pamathur, Q2FY25)
		var (
			backupName2 = fmt.Sprintf("%s-%v-2", BackupNamePrefix, time.Now().Unix())
			backupName  = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			restoreName = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			clusterUid  string
		)
		Step("Create a cluster & backup object with admin and Share with User2", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with [%s] ctx", SourceClusterName, pxbUsers[0].name))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid using [%s] ctx", SourceClusterName, pxbUsers[0].name))

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName, pxbUsers[0].name))

			// Share the source cluster to testuser2
			_, err = ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, true)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with [%s] user using [%s] ctx", SourceClusterName, pxbUsers[2].name, pxbUsers[0].name))
		})
		Step("Create a backup object with User2 and restore with admin Backup", func() {
			// Create Backup
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName2, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s] using [%s] ctx", backupName2, pxbUsers[2].name))

			// Create Restore
			restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
			namespaceMapping := make(map[string]string)
			for _, namespace := range bkpNamespaces {
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s] using [%s] ctx", backupName, pxbUsers[2].name))
		})
	})

	JustAfterEach(func() {
		defer EndPxBackupTorpedoTest(scheduledAppContexts)

		opts := make(map[string]bool)
		opts[SkipClusterScopedObjects] = true
		log.InfoD("Deleting deployed applications")
		DestroyApps(scheduledAppContexts, opts)

		restoreResp, err := Inst().Backup.EnumerateRestore(pxbUsers[0].ctx, &api.RestoreEnumerateRequest{
			OrgId: BackupOrgID,
		})
		dash.VerifyFatal(err, nil, "Enumerating restores")
		for _, restore := range restoreResp.GetRestores() {
			log.InfoD("Deleting restore")
			err = DeleteRestore(restore.GetName(), BackupOrgID, pxbUsers[0].ctx)
			statusErr, ok := status.FromError(err)
			if !ok && statusErr.Code() == codes.NotFound {
				log.Infof("Restore [%s] already deleted", restore.GetName())
				continue
			}
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting restore [%s]", restore.GetName()))
		}

		scheduleResp, err := Inst().Backup.EnumerateBackupSchedule(pxbUsers[0].ctx, &api.BackupScheduleEnumerateRequest{
			OrgId: BackupOrgID,
		})
		dash.VerifyFatal(err, nil, "Enumerating backupschedules")
		for _, schedule := range scheduleResp.GetBackupSchedules() {
			log.InfoD("Deleting schedule")
			err = DeleteScheduleWithUIDAndWait(schedule.GetName(), schedule.GetUid(), schedule.GetClusterRef().GetName(), schedule.GetClusterRef().GetUid(), BackupOrgID, pxbUsers[0].ctx)
			statusErr, ok := status.FromError(err)
			if !ok && statusErr.Code() == codes.NotFound {
				log.Infof("Backup Schedule [%s] already deleted", schedule.GetName())
				continue
			}
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting schedule [%s]", schedule.GetName()))
		}

		backupResp, err := Inst().Backup.EnumerateBackup(pxbUsers[0].ctx, &api.BackupEnumerateRequest{
			OrgId: BackupOrgID,
		})
		dash.VerifyFatal(err, nil, "Enumerating backups")
		for _, backup := range backupResp.GetBackups() {
			log.InfoD("Deleting backup")
			err = DeleteBackupAndWaitForCompletion(backup.GetName(), backup.GetUid(), BackupOrgID, pxbUsers[0].ctx)
			statusErr, ok := status.FromError(err)
			if !ok && statusErr.Code() == codes.NotFound {
				log.Infof("Backup [%s] already deleted", backup.GetName())
				continue
			}
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backup.GetName()))
		}

		CleanupCloudSettingsAndClusters(backupLocationMap, cloudCredName, cloudCredUID, pxbUsers[0].ctx)
		for i := 1; i <= 2; i++ {
			err := backup.DeleteUser(pxbUsers[i].name)
			log.FailOnError(err, "Failed to delete user - %s", pxbUsers[i].name)
		}
	})

})

var _ = Describe("{ClusterShareTestcasesWithAppUsers}", Label(TestCaseLabelsMap[ClusterShare]...), func() {
	var (
		scheduledAppContexts []*scheduler.Context
		bkpNamespaces        []string
		providers            []string = GetBackupProviders()
		cloudCredName        string
		cloudCredUID         string
		backupLocationUID    string
		bkpLocationName      string
		backupLocationMap    map[string]string
		pxbUsers             map[int]user
	)
	JustBeforeEach(func() {
		backupLocationMap = make(map[string]string)
		scheduledAppContexts = make([]*scheduler.Context, 0)
		bkpNamespaces = make([]string, 0)
		cloudCredName, cloudCredUID, backupLocationUID, bkpLocationName = "", "", "", ""

		// Schedule an Application
		appContexts := ScheduleApplications(TaskNamePrefix)
		for _, ctx := range appContexts {
			ctx.ReadinessTimeout = AppReadinessTimeout
			namespace := GetAppNamespace(ctx, TaskNamePrefix)
			bkpNamespaces = append(bkpNamespaces, namespace)
			scheduledAppContexts = append(scheduledAppContexts, ctx)
		}

		// Validate the application
		ValidateApplications(scheduledAppContexts)

		// Create two test users
		var err error
		numberOfUsers := 2
		log.InfoD("Creating %d users", numberOfUsers)
		testusers := CreateUsers(numberOfUsers)
		pxbUsers = make(map[int]user)

		adminCtx, err := backup.GetAdminCtxFromSecret()
		log.FailOnError(err, "Fetching admin user ctx")

		pxbUsers[0] = user{name: "ADMIN USER", ctx: adminCtx}
		//pxbUsers = map[int]user{1: {name: "testuser1"}, 2: {name: "testuser2"}}
		for index, testuser := range testusers {
			ctxNonAdmin, err := backup.GetNonAdminCtx(testuser, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			pxbUsers[index+1] = user{testuser, ctxNonAdmin}
		}

		// Create backup location and cloud setting with admin user
		log.InfoD("Creating backup location and cloud setting with admin user")
		for _, provider := range providers {
			cloudCredName = fmt.Sprintf("%s-%s-%v", "cred", provider, time.Now().Unix())
			bkpLocationName = fmt.Sprintf("autogenerated-backup-location-%v", time.Now().Unix())
			cloudCredUID = uuid.New()
			backupLocationUID = uuid.New()
			backupLocationMap[backupLocationUID] = bkpLocationName
			err := CreateCloudCredential(provider, cloudCredName, cloudCredUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of cloud credential named [%s] for org [%s] with [%s] as provider", cloudCredName, BackupOrgID, provider))
			err = CreateBackupLocationWithContext(provider, bkpLocationName, backupLocationUID, cloudCredName, cloudCredUID, getGlobalBucketName(provider), BackupOrgID, "", pxbUsers[0].ctx, true)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creating backup location %s", bkpLocationName))
		}

		// Share the Backup Location with testUser2
		log.InfoD("Sharing backup location with user2")
		err = AddBackupLocationOwnership(bkpLocationName, backupLocationUID, []string{pxbUsers[2].name}, nil, Read, Invalid, pxbUsers[0].ctx)
		log.FailOnError(err, "failed to update backup location [%v] ownership", bkpLocationName)
	})

	It("VerifyClusterShareByAdminToAnyUserAndVerifyBackupOperation", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareByAdminToAnyUserAndVerifyBackupOperation", "VerifyClusterShareByAdminToAnyUserAndVerifyBackupOperation", nil, 301024, Sgajawada, Q2FY25)

		var (
			backupName          = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			testUser2backupName = fmt.Sprintf("%s-user2-%v", BackupNamePrefix, time.Now().Unix())
			clusterUid          string
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Create a Backup with Admin User", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
		})

		Step("Share the admin created cluster with User2", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using admin ctx", SourceClusterName, pxbUsers[2].name))
		})

		Step("Verify the cluster access with User2", func() {
			err := VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(pxbUsers[2].ctx, pxbUsers[2].name, CommonPassword, clusterUid)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verify pxb objects for %v user", pxbUsers[2].name))
		})

		Step("Create a backup on the shared cluster with testuser2", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(testUser2backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", testUser2backupName))
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			testUser2backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, testUser2backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser2backupName)

			log.InfoD("Deleting backup created by user2")
			err = DeleteBackupAndWaitForCompletion(testUser2backupName, testUser2backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser2backupName))

			backupUID, err := backupDriver.GetBackupUID(pxbUsers[0].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup created by admin")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			log.InfoD("Deleting cluster created by admin")
			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})
	})

	It("VerifyClusterShareByAdminToAnyUserAndVerifyBackupScheduleOperation", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareByAdminToAnyUserAndVerifyBackupScheduleOperation", "VerifyClusterShareByAdminToAnyUserAndVerifyBackupScheduleOperation", nil, 301025, Sgajawada, Q2FY25)

		var (
			backupName                  = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			testUser2backupScheduleName = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			firstScheduleBackupName     string
			clusterUid                  string
			periodicSchedulePolicyName  string
			periodicSchedulePolicyUid   string
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Create a Backup with Admin User", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
		})

		Step("Share the admin created cluster with User2", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using admin ctx", SourceClusterName, pxbUsers[2].name))
		})

		Step("Verify the cluster access with User2", func() {
			err := VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(pxbUsers[2].ctx, pxbUsers[2].name, CommonPassword, clusterUid)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verify pxb objects for %v user", pxbUsers[2].name))
		})

		Step("Create BackupSchedule on the shared cluster with testuser2", func() {
			// Create Schedule Policy With Admin Context and Share it with User2
			periodicSchedulePolicyName = fmt.Sprintf("%s-%s", "periodic", RandomString(5))
			periodicSchedulePolicyUid = uuid.New()
			periodicSchedulePolicyInterval := int64(15)
			err := CreateBackupScheduleIntervalPolicy(5, periodicSchedulePolicyInterval, 5, periodicSchedulePolicyName, periodicSchedulePolicyUid, BackupOrgID, pxbUsers[0].ctx, false, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of periodic schedule policy of interval [%v] minutes named [%s] ", periodicSchedulePolicyInterval, periodicSchedulePolicyName))

			err = AddSchedulePolicyOwnership(periodicSchedulePolicyName, periodicSchedulePolicyUid, []string{pxbUsers[2].name}, nil, Read, Invalid, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying updation of ownership for schedulepolicy - %s", periodicSchedulePolicyName))

			// Take Backup Schedule
			log.InfoD("Taking schedule backup of multiple namespaces")
			testUser2backupScheduleName = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(testUser2backupScheduleName, SourceClusterName, clusterUid, bkpLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of scheduled backup with schedule name [%s]", testUser2backupScheduleName))

			// Get the first schedule backup name
			firstScheduleBackupName, err = GetFirstScheduleBackupName(pxbUsers[2].ctx, testUser2backupScheduleName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of first scheduled backup [%s] with schedule name [%s]", firstScheduleBackupName, testUser2backupScheduleName))
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			firstScheduleBackupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, firstScheduleBackupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", firstScheduleBackupName)

			log.InfoD("Deleting first schedule backup created by user2")
			err = DeleteBackupAndWaitForCompletion(firstScheduleBackupName, firstScheduleBackupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", firstScheduleBackupName))

			backupUID, err := backupDriver.GetBackupUID(pxbUsers[0].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup created by admin")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			log.InfoD("Deleting backup schedule [%s]", testUser2backupScheduleName)
			scheduleUID, err := GetScheduleUID(testUser2backupScheduleName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backupschedule uid", testUser2backupScheduleName))

			err = DeleteScheduleWithUIDAndWait(testUser2backupScheduleName, scheduleUID, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of backup schedule [%s]", testUser2backupScheduleName))

			log.InfoD("Deleting schedule policy [%s]", periodicSchedulePolicyName)
			schedulePolicyDeleteRequest := &api.SchedulePolicyDeleteRequest{
				Name:  periodicSchedulePolicyName,
				Uid:   periodicSchedulePolicyUid,
				OrgId: BackupOrgID,
			}
			_, err = Inst().Backup.DeleteSchedulePolicy(pxbUsers[0].ctx, schedulePolicyDeleteRequest)
			log.FailOnError(err, "failed to delete schedule policy %s of the user %s", periodicSchedulePolicyName, pxbUsers[2].name)

			log.InfoD("Deleting cluster created by admin")
			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})
	})

	It("VerifyClusterShareByAdminToAnyUserAndVerifyBackupAndRestoreOperation", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareByAdminToAnyUserAndVerifyBackupRestoreOperation", "VerifyClusterShareByAdminToAnyUserAndVerifyBackupRestoreOperation", nil, 301026, Dbinnal, Q2FY25)

		var (
			testUser2backupName = fmt.Sprintf("%s-user2-%v", BackupNamePrefix, time.Now().Unix())
			clusterUid          string
			restoreName         = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Share the admin created cluster with User2", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using admin ctx", SourceClusterName, pxbUsers[2].name))
		})

		Step("Verify the cluster access with User2", func() {
			err := VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(pxbUsers[2].ctx, pxbUsers[2].name, CommonPassword, clusterUid)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verify pxb objects for %v user", pxbUsers[2].name))
		})

		Step("Create a backup on the shared cluster with testuser2", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(testUser2backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", testUser2backupName))
		})

		Step("Create a restore on the shared cluster with testuser2", func() {
			log.InfoD(fmt.Sprintf("Taking restore of the backup [%v]", testUser2backupName))
			err := CreateRestore(restoreName, testUser2backupName, make(map[string]string), SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", testUser2backupName))
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			testUser2backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, testUser2backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser2backupName)

			log.InfoD("Deleting backup created by user2")
			err = DeleteBackupAndWaitForCompletion(testUser2backupName, testUser2backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser2backupName))

			log.InfoD("Deleting restore [%s] created by user2", restoreName)
			err = DeleteRestore(restoreName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting restore [%s]", restoreName))

			log.InfoD("Deleting cluster created by admin")
			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})
	})

	It("VerifyClusterShareByAdminToMultipleUsersAndVerifyBackupScheduleOperation", func() {
		StartPxBackupTorpedoTest("VerifyClusterShareByAdminToMultipleUsersAndVerifyBackupScheduleOperation", "Cluster share by admin to more than one user, create backup and verify backups are only visible to owners only", nil, 301027, Sgajawada, Q2FY25)

		var (
			backupName                       = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			restoreName                      = fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			testUser2backupScheduleName      = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			testUser1backupScheduleName      = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			namespaceMapping                 = make(map[string]string)
			testUser2FirstScheduleBackupName string
			testUser1FirstScheduleBackupName string
			clusterUid                       string
			periodicSchedulePolicyName       string
			periodicSchedulePolicyUid        string
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Create a Backup with Admin User", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
		})

		Step("Share the admin created cluster with User2 and User1", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name, pxbUsers[1].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with test users using admin ctx", SourceClusterName))
		})

		Step("Verify the cluster access with User2", func() {
			err := VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(pxbUsers[2].ctx, pxbUsers[2].name, CommonPassword, clusterUid)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verify pxb objects for %v user", pxbUsers[2].name))
		})

		Step("Create Schedule Policy with testuser2 and Share it with testuser1", func() {
			// Create Schedule Policy
			periodicSchedulePolicyName = fmt.Sprintf("%s-%s", "periodic", RandomString(5))
			periodicSchedulePolicyUid = uuid.New()
			periodicSchedulePolicyInterval := int64(15)
			err := CreateBackupScheduleIntervalPolicy(5, periodicSchedulePolicyInterval, 5, periodicSchedulePolicyName, periodicSchedulePolicyUid, BackupOrgID, pxbUsers[0].ctx, false, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of periodic schedule policy of interval [%v] minutes named [%s] ", periodicSchedulePolicyInterval, periodicSchedulePolicyName))

			err = AddSchedulePolicyOwnership(periodicSchedulePolicyName, periodicSchedulePolicyUid, []string{pxbUsers[1].name, pxbUsers[2].name}, nil, Read, Invalid, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying updation of ownership for schedulepolicy - %s", periodicSchedulePolicyName))

		})

		Step("Create BackupSchedule on the shared cluster with testuser2", func() {
			// Take Backup Schedule
			var err error
			log.InfoD("Taking schedule backup of multiple namespaces")
			testUser2backupScheduleName = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(testUser2backupScheduleName, SourceClusterName, clusterUid, bkpLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of scheduled backup with schedule name [%s]", testUser2backupScheduleName))

			// Get the first schedule backup name
			testUser2FirstScheduleBackupName, err = GetFirstScheduleBackupName(pxbUsers[2].ctx, testUser2backupScheduleName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of first scheduled backup [%s] with schedule name [%s]", testUser2FirstScheduleBackupName, testUser2backupScheduleName))
		})

		Step("Restore the Schedule backup to another namespace", func() {
			// Create Restore
			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", RandomString(10), "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			log.InfoD("Namespace mapping is %v:", namespaceMapping)
			err := CreateRestore(restoreName, testUser2FirstScheduleBackupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
		})

		Step("Verify the cluster access with User1", func() {
			err := VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(pxbUsers[1].ctx, pxbUsers[1].name, CommonPassword, clusterUid)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verify pxb objects for %v user", pxbUsers[1].name))
		})

		Step("Sharing backup location with user1", func() {
			log.InfoD("Sharing backup location with user1")
			err := AddBackupLocationOwnership(bkpLocationName, backupLocationUID, []string{pxbUsers[1].name}, nil, Read, Invalid, pxbUsers[0].ctx)
			log.FailOnError(err, "failed to update backup location [%v] ownership", bkpLocationName)
		})

		Step("Create BackupSchedule on the shared cluster with testuser1", func() {
			// Take Backup Schedule
			var err error
			log.InfoD("Taking schedule backup of multiple namespaces")
			testUser1backupScheduleName = fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(testUser1backupScheduleName, SourceClusterName, clusterUid, bkpLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of scheduled backup with schedule name [%s]", testUser1backupScheduleName))

			// Get the first schedule backup name
			testUser1FirstScheduleBackupName, err = GetFirstScheduleBackupName(pxbUsers[1].ctx, testUser1backupScheduleName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of first scheduled backup [%s] with schedule name [%s]", testUser1FirstScheduleBackupName, testUser1backupScheduleName))
		})

		Step("pxbUsers Should enumerate their own backupobjects", func() {
			testUser1BackupObjects, err := GetAllBackupsForUser(pxbUsers[1].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllOwnedBackupsFromCluster %s for user %s", SourceClusterName, pxbUsers[1].name)
			for _, backupname := range testUser1BackupObjects {
				bkpInspectRequest := &api.BackupInspectRequest{
					OrgId: BackupOrgID,
					Name:  backupname,
				}
				resp, err := Inst().Backup.InspectBackup(pxbUsers[1].ctx, bkpInspectRequest)
				log.FailOnError(err, "failed to inspect backup [%v]", backupname)

				// First fetch the client ID of the user1
				clientID, err := backup.FetchIDOfUser(pxbUsers[1].name)
				if err != nil {
					log.FailOnError(err, "Failed to fetch the userID for %v", pxbUsers[1].name)
				}
				dash.VerifyFatal(resp.Backup.GetOwnership().GetOwner(), clientID, fmt.Sprintf("Verfy owner for the backup %v", backupname))
			}

			testUser2BackupObjects, err := GetAllBackupsForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllOwnedBackupsFromCluster %s for user %s", SourceClusterName, pxbUsers[2].name)
			for _, backupname := range testUser2BackupObjects {
				bkpInspectRequest := &api.BackupInspectRequest{
					OrgId: BackupOrgID,
					Name:  backupname,
				}
				resp, err := Inst().Backup.InspectBackup(pxbUsers[2].ctx, bkpInspectRequest)
				log.FailOnError(err, "failed to inspect backup [%v]", backupname)

				// First fetch the client ID of the user2
				clientID, err := backup.FetchIDOfUser(pxbUsers[2].name)
				if err != nil {
					log.FailOnError(err, "Failed to fetch the userID for %v", pxbUsers[2].name)
				}
				dash.VerifyFatal(resp.Backup.GetOwnership().GetOwner(), clientID, fmt.Sprintf("Verfy owner for the backup %v", backupname))
			}
		})

		Step("Cleanup 1", func() {
			backupDriver := Inst().Backup
			// Delete user1 schedule backup
			testUser1FirstScheduleBackupUID, err := backupDriver.GetBackupUID(pxbUsers[1].ctx, testUser1FirstScheduleBackupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser1FirstScheduleBackupName)

			log.InfoD("Deleting first schedule backup created by user1")
			err = DeleteBackupAndWaitForCompletion(testUser1FirstScheduleBackupName, testUser1FirstScheduleBackupUID, BackupOrgID, pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser1FirstScheduleBackupName))

			backupUID, err := backupDriver.GetBackupUID(pxbUsers[0].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			// Delete user2 schedule backup
			testUser2FirstScheduleBackupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, testUser2FirstScheduleBackupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser2FirstScheduleBackupName)

			log.InfoD("Deleting first schedule backup created by user2")
			err = DeleteBackupAndWaitForCompletion(testUser2FirstScheduleBackupName, testUser2FirstScheduleBackupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser2FirstScheduleBackupName))

			// Delete admin created backup
			log.InfoD("Deleting backup created by admin")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			// Delete user2 backup schedule
			log.InfoD("Deleting backup schedule [%s]", testUser2backupScheduleName)
			scheduleUID, err := GetScheduleUID(testUser2backupScheduleName, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backupschedule uid", testUser2backupScheduleName))

			err = DeleteScheduleWithUIDAndWait(testUser2backupScheduleName, scheduleUID, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of backup schedule [%s]", testUser2backupScheduleName))

			// Delete user1 backup schedule
			log.InfoD("Deleting backup schedule [%s]", testUser1backupScheduleName)
			scheduleUID, err = GetScheduleUID(testUser1backupScheduleName, BackupOrgID, pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backupschedule uid", testUser1backupScheduleName))

			err = DeleteScheduleWithUIDAndWait(testUser1backupScheduleName, scheduleUID, SourceClusterName, clusterUid, BackupOrgID, pxbUsers[1].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of backup schedule [%s]", testUser1backupScheduleName))

			// Delete Schedule Policy Object
			log.InfoD("Deleting schedule policy [%s]", periodicSchedulePolicyName)
			schedulePolicyDeleteRequest := &api.SchedulePolicyDeleteRequest{
				Name:  periodicSchedulePolicyName,
				Uid:   periodicSchedulePolicyUid,
				OrgId: BackupOrgID,
			}
			_, err = Inst().Backup.DeleteSchedulePolicy(pxbUsers[0].ctx, schedulePolicyDeleteRequest)
			log.FailOnError(err, "failed to delete schedule policy %s of the user %s", periodicSchedulePolicyName, pxbUsers[2].name)
		})

		Step("UnShare the admin created cluster with User2 and User1", func() {
			_, err := UnShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name, pxbUsers[1].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying unshare of source [%s] cluster with test users using admin ctx", SourceClusterName))
		})

		Step("Cleanup 2", func() {

			// Delete Cluster Object
			log.InfoD("Deleting cluster created by admin")
			err := DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})
	})

	It("VerifyCascadingClusterShareOperationIsDenied.", func() {
		StartPxBackupTorpedoTest("VerifyCascadingClusterShareOperationIsDenied", "As Cluster Shared user verify Cluster share operation is denied.", nil, 301032, Sgajawada, Q2FY25)
		var (
			clusterUid string
			userKey    = len(pxbUsers)
		)

		Step("Create App Admin User", func() {
			users := CreateUsers(1)

			// Add app admin role(ApplicationOwner) to the user
			err := backup.AddRoleToUser(users[0], backup.ApplicationOwner, fmt.Sprintf("Adding %v role to %s", backup.ApplicationOwner, users[0]))
			log.FailOnError(err, "Failed to add role %s to the user %s", backup.ApplicationOwner, users[0])

			nonAdminCtx, err := backup.GetNonAdminCtx(users[0], CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			pxbUsers[userKey] = user{name: users[0], ctx: nonAdminCtx}
		})

		Step("Create a cluster object with app admin user", func() {
			err := AddSourceCluster(pxbUsers[userKey].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with app admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[userKey].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Share the Cluster with User1", func() {
			_, err := ShareCluster(pxbUsers[userKey].ctx, SourceClusterName, clusterUid, []string{pxbUsers[1].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with test user1 using app admin ctx", SourceClusterName))
		})

		Step("Again Share the Cluster with User2 with User1(Shared Cluster User) context", func() {
			_, err := ShareCluster(pxbUsers[1].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Verifying [%s] cluster share by shared user[%s] is not allowed", SourceClusterName, pxbUsers[1].name))
		})

		Step("Cleanup", func() {
			// Delete Cluster Object
			log.InfoD("Deleting cluster created by app admin")
			err := DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[userKey].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))
		})

	})

	It("VerifyClusterDeletionFailedBySharedClusterUser", func() {
		StartPxBackupTorpedoTest("VerifyClusterDeletionFailedBySharedClusterUser", "As Cluster shared user verify Delete Cluster Fails.", nil, 301033, Sgajawada, Q2FY25)
		var (
			backupName          = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			testUser2backupName = fmt.Sprintf("%s-user2-%v", BackupNamePrefix, time.Now().Unix())
			clusterUid          string
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Create a Backup with Admin User", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
		})

		Step("Share the admin created cluster with User2", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using admin ctx", SourceClusterName, pxbUsers[2].name))
		})

		Step("Verify the cluster access with User2", func() {
			clusterInspectReq := &api.ClusterInspectRequest{
				OrgId:          BackupOrgID,
				Name:           SourceClusterName,
				Uid:            clusterUid,
				IncludeSecrets: true,
			}
			_, err := Inst().Backup.InspectCluster(pxbUsers[2].ctx, clusterInspectReq)
			log.FailOnError(err, "failed to inspect cluster %s", SourceClusterName)

			backupObjects, err := GetAllBackupsForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllBackupsForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(backupObjects), 0, fmt.Sprintf("Verifying all backups in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))

			restoreObjects, err := GetAllRestoresForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllRestoresForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(restoreObjects), 0, fmt.Sprintf("Verifying all restores in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))

			backupScheduleObjects, err := GetAllBackupSchedulesForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllBackupSchedulesForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(backupScheduleObjects), 0, fmt.Sprintf("Verifying all backup schedules in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))
		})

		Step("Create a backup on the shared cluster with testuser2", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(testUser2backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", testUser2backupName))

			bkpInspectRequest := &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  testUser2backupName,
			}
			resp, err := Inst().Backup.InspectBackup(pxbUsers[2].ctx, bkpInspectRequest)
			log.FailOnError(err, "failed to inspect backup [%v]", testUser2backupName)

			// First fetch the client ID of the user2
			clientID, err := backup.FetchIDOfUser(pxbUsers[2].name)
			if err != nil {
				log.FailOnError(err, "Failed to fetch the userID for %v", pxbUsers[2].name)
			}
			dash.VerifyFatal(resp.Backup.GetOwnership().GetOwner(), clientID, fmt.Sprintf("Verfy owner for the backup %v", testUser2backupName))
		})

		Step("Perform delete of this cluster from testUser2", func() {
			log.InfoD("Deleting cluster created by admin using testUser2 ctx")
			err := DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[2].ctx, false)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("Deleting Cluster [%s] with testUser2", SourceClusterName))
			dash.VerifyFatal(status.Code(err), codes.PermissionDenied, fmt.Sprintf("Verify %v error code when deleting the cluster %s with user %s", codes.PermissionDenied, SourceClusterName, pxbUsers[2].name))
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			testUser2backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, testUser2backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser2backupName)

			log.InfoD("Deleting backup created by user2")
			err = DeleteBackupAndWaitForCompletion(testUser2backupName, testUser2backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser2backupName))

			backupUID, err := backupDriver.GetBackupUID(pxbUsers[0].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup created by admin")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			log.InfoD("Deleting cluster created by admin")
			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))

		})
	})

	It("VerifyObjectsAccessPostUnShareTheCluster", func() {
		StartPxBackupTorpedoTest("VerifyObjectsAccessPostUnShareTheCluster", "Revoke Cluster share to a user verify access to objects by user.", nil, 301034, Sgajawada, Q2FY25)
		var (
			backupName          = fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			testUser2backupName = fmt.Sprintf("%s-user2-%v", BackupNamePrefix, time.Now().Unix())
			clusterUid          string
		)

		Step("Create a cluster object with Admin User ", func() {
			err := AddSourceCluster(pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with admin ctx", SourceClusterName))

			clusterUid, err = Inst().Backup.GetClusterUID(pxbUsers[0].ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Create a Backup with Admin User", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
		})

		Step("Share the admin created cluster with User2", func() {
			_, err := ShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user using admin ctx", SourceClusterName, pxbUsers[2].name))
		})

		Step("Verify the cluster access with User2", func() {
			clusterInspectReq := &api.ClusterInspectRequest{
				OrgId:          BackupOrgID,
				Name:           SourceClusterName,
				Uid:            clusterUid,
				IncludeSecrets: true,
			}
			_, err := Inst().Backup.InspectCluster(pxbUsers[2].ctx, clusterInspectReq)
			log.FailOnError(err, "failed to inspect cluster %s", SourceClusterName)

			backupObjects, err := GetAllBackupsForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllBackupsForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(backupObjects), 0, fmt.Sprintf("Verifying all backups in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))

			restoreObjects, err := GetAllRestoresForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllRestoresForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(restoreObjects), 0, fmt.Sprintf("Verifying all restores in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))

			backupScheduleObjects, err := GetAllBackupSchedulesForUser(pxbUsers[2].name, CommonPassword)
			log.FailOnError(err, "failed to GetAllBackupSchedulesForUser %s for user %s", SourceClusterName, pxbUsers[2].name)
			dash.VerifyFatal(len(backupScheduleObjects), 0, fmt.Sprintf("Verifying all backup schedules in source [%s] cluster with %s user ", SourceClusterName, pxbUsers[2].name))
		})

		Step("Create a backup on the shared cluster with testuser2", func() {
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err := CreateBackup(testUser2backupName, SourceClusterName, bkpLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", testUser2backupName))

			bkpInspectRequest := &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  testUser2backupName,
			}
			resp, err := Inst().Backup.InspectBackup(pxbUsers[2].ctx, bkpInspectRequest)
			log.FailOnError(err, "failed to inspect backup [%v]", testUser2backupName)

			// First fetch the client ID of the user2
			clientID, err := backup.FetchIDOfUser(pxbUsers[2].name)
			if err != nil {
				log.FailOnError(err, "Failed to fetch the userID for %v", pxbUsers[2].name)
			}
			dash.VerifyFatal(resp.Backup.GetOwnership().GetOwner(), clientID, fmt.Sprintf("Verfy owner for the backup %v", testUser2backupName))
		})

		Step("UnShare the admin created cluster with User2", func() {
			_, err := UnShareCluster(pxbUsers[0].ctx, SourceClusterName, clusterUid, []string{pxbUsers[2].name}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying unshare of source [%s] cluster with testuser2 using admin ctx", SourceClusterName))
		})

		Step("Verify from admin user that backup created by testUser2 is still accessible", func() {
			bkpInspectRequest := &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  testUser2backupName,
			}
			_, err := Inst().Backup.InspectBackup(pxbUsers[0].ctx, bkpInspectRequest)
			log.FailOnError(err, "failed to inspect backup [%v] with admin ctx", testUser2backupName)
		})

		Step("Verify that cluster is no longer accessible with testUser2", func() {
			clusterInspectReq := &api.ClusterInspectRequest{
				OrgId:          BackupOrgID,
				Name:           SourceClusterName,
				Uid:            clusterUid,
				IncludeSecrets: true,
			}
			_, err := Inst().Backup.InspectCluster(pxbUsers[2].ctx, clusterInspectReq)
			log.FailOnNoError(err, "failed to inspect cluster %s", SourceClusterName)
		})

		Step("Verify that backups are accessible from shared cluster that was revoked", func() {
			bkpInspectRequest := &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  testUser2backupName,
			}
			_, err := Inst().Backup.InspectBackup(pxbUsers[2].ctx, bkpInspectRequest)
			log.FailOnError(err, "failed to inspect backup [%v] with testuser2 ctx", testUser2backupName)
		})

		Step("Cleanup", func() {
			backupDriver := Inst().Backup
			testUser2backupUID, err := backupDriver.GetBackupUID(pxbUsers[2].ctx, testUser2backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", testUser2backupName)

			log.InfoD("Deleting backup created by user2")
			err = DeleteBackupAndWaitForCompletion(testUser2backupName, testUser2backupUID, BackupOrgID, pxbUsers[2].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", testUser2backupName))

			backupUID, err := backupDriver.GetBackupUID(pxbUsers[0].ctx, backupName, BackupOrgID)
			log.FailOnError(err, "Failed while trying to get backup UID for - [%s]", backupName)

			log.InfoD("Deleting backup created by admin")
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, pxbUsers[0].ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))

			log.InfoD("Deleting cluster created by admin")
			err = DeleteClusterWithUID(SourceClusterName, clusterUid, BackupOrgID, pxbUsers[0].ctx, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting Cluster [%s]", SourceClusterName))

		})
	})

	JustAfterEach(func() {
		defer EndPxBackupTorpedoTest(scheduledAppContexts)

		opts := make(map[string]bool)
		opts[SkipClusterScopedObjects] = true
		log.InfoD("Deleting deployed applications")
		DestroyApps(scheduledAppContexts, opts)

		CleanupCloudSettingsAndClusters(backupLocationMap, cloudCredName, cloudCredUID, pxbUsers[0].ctx)
		for i := 1; i < len(pxbUsers); i++ {
			err := backup.DeleteUser(pxbUsers[i].name)
			log.FailOnError(err, "Failed to delete user - %s", pxbUsers[i].name)
		}
	})

})

// Share cluster from AppAdmin and AppUser to any user with backups, schedules and restore validations
var _ = Describe("{ClusterShareFromAppAdminAndAppUserToAnyUser}", Label(TestCaseLabelsMap[ClusterShare]...), func() {
	var (
		backupLocationName         string
		scheduledAppContexts       []*scheduler.Context
		bkpNamespaces              []string
		backupsList                []string
		restoreList                []string
		cloudCredName              string
		cloudCredUID               string
		backupLocationUID          string
		backupLocationMap          map[string]string
		clusterUid                 string
		appAdminRole               backup.PxBackupRole = backup.ApplicationOwner
		periodicSchedulePolicyName string
		periodicSchedulePolicyUid  string
		appAdminUser               string
		testUser1                  string
		testUser2                  string
	)

	JustBeforeEach(func() {
		cloudCredName, cloudCredUID, backupLocationUID, backupLocationName, periodicSchedulePolicyName, periodicSchedulePolicyUid = "", "", "", "", "", ""
		backupLocationMap = make(map[string]string)
		scheduledAppContexts = make([]*scheduler.Context, 0)
		backupsList = make([]string, 0)
		restoreList = make([]string, 0)
		bkpNamespaces = make([]string, 0)
		bkpNamespaces = make([]string, 0)
		appAdminUser = CreateUsers(1)[0]
		testUser1 = CreateUsers(1)[0]
		testUser2 = CreateUsers(1)[0]

		err := backup.AddRoleToUser(appAdminUser, appAdminRole, fmt.Sprintf("Adding %v role to %s", appAdminRole, appAdminUser))
		log.FailOnError(err, "failed to add role %s to the user %s", appAdminRole, appAdminUser)

		// Schedule an Application
		appContexts := ScheduleApplications(TaskNamePrefix)
		for _, ctx := range appContexts {
			ctx.ReadinessTimeout = AppReadinessTimeout
			namespace := GetAppNamespace(ctx, TaskNamePrefix)
			bkpNamespaces = append(bkpNamespaces, namespace)
			scheduledAppContexts = append(scheduledAppContexts, ctx)
		}
		// Validate the application
		ValidateApplications(scheduledAppContexts)

		// Create Backup Location and Cloud Setting
		adminContext, err := backup.GetAdminCtxFromSecret()
		log.FailOnError(err, "Fetching admin user ctx")
		log.InfoD("Creating backup location and cloud setting with admin user")
		backupLocationProviders := GetBackupProviders()
		for _, provider := range backupLocationProviders {
			cloudCredName = fmt.Sprintf("%s-cred-%v", provider, RandomString(10))
			cloudCredUID = uuid.New()
			err := CreateCloudCredential(provider, cloudCredName, cloudCredUID, BackupOrgID, adminContext)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of cloud credential named [%s] for org [%s]  as provider %s", cloudCredName, BackupOrgID, provider))

			backupLocationName = fmt.Sprintf("%s-backup-location-%v", provider, RandomString(10))
			backupLocationUID = uuid.New()
			backupLocationMap[backupLocationUID] = backupLocationName
			err = CreateBackupLocationWithContext(provider, backupLocationName, backupLocationUID, cloudCredName, cloudCredUID, getGlobalBucketName(provider), BackupOrgID, "", adminContext, true)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creating backup location %s", backupLocationName))
		}

		// Share backup location with app user
		err = AddBackupLocationOwnership(backupLocationName, backupLocationUID, []string{appAdminUser, testUser1, testUser2}, nil, Read, Invalid, adminContext)
		dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of backup location [%s] with users [%v] using admin ctx", backupLocationName, []string{appAdminUser, testUser1, testUser2}))

		// Create Schedule Policy
		periodicSchedulePolicyName = fmt.Sprintf("%s-%s", "periodic", RandomString(5))
		periodicSchedulePolicyUid = uuid.New()
		periodicSchedulePolicyInterval := int64(30)
		err = CreateBackupScheduleIntervalPolicy(5, periodicSchedulePolicyInterval, 5, periodicSchedulePolicyName, periodicSchedulePolicyUid, BackupOrgID, adminContext, false, false)
		dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of periodic schedule policy of interval [%v] minutes named [%s] ", periodicSchedulePolicyInterval, periodicSchedulePolicyName))

		// Share schedule policy with app user
		err = AddSchedulePolicyOwnership(periodicSchedulePolicyName, periodicSchedulePolicyUid, []string{testUser1, testUser2}, nil, Read, Invalid, adminContext)
		dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of schedule policy [%s] with [%s] users", periodicSchedulePolicyName, []string{testUser1, testUser2}))
	})

	// This test case verifies the cluster share operation from app admin any user with backups, schedules and restore validations
	It("Verify cluster share from app admin to any user with backups, schedules and restore creation", func() {
		StartPxBackupTorpedoTest("VerifyClusterSharefromAppAdminToAnyUSer", "Verify Cluster share from app admin to any user with backups, schedules ad restores", nil, 301028, Dbinnal, Q2FY25)

		Step("Create a cluster object with appadmin and Share with appuser", func() {
			appAdminCtx, err := backup.GetNonAdminCtx(appAdminUser, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			err = AddSourceCluster(appAdminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, appAdminUser))
			log.InfoD("Cluster [%s] created successfully", SourceClusterName)

			clusterUid, err = Inst().Backup.GetClusterUID(appAdminCtx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", appAdminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
			backupsList = append(backupsList, backupName)

			// Share the source cluster to testuser1
			_, err = ShareCluster(appAdminCtx, SourceClusterName, clusterUid, []string{testUser1, testUser2}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s users using %s user", SourceClusterName, []string{testUser1, testUser2}, appAdminUser))

			// Verify backups/restores/schedules are not accessible to shared user
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(testUser1Ctx, testUser1, CommonPassword, clusterUid)
		})

		Step("Verify backup creation with appUser", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			// Delete backup
			backupUID, err := Inst().Backup.GetBackupUID(testUser1Ctx, backupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupName))
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))
		})

		Step("Create a backup schedule with app user on shared cluster", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule
			scheduleName := fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(scheduleName, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", scheduleName))
			log.InfoD("Backup schedule [%s] created successfully", scheduleName)
		})

		Step("Create backup and restore it on the same cluster(Shared Cluster) with app user", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			restoreName := fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping := make(map[string]string)

			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", namespace, "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			// Create Restore
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, testUser1Ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
			restoreList = append(restoreList, restoreName)

			// Delete backup
			backupUID, err := Inst().Backup.GetBackupUID(testUser1Ctx, backupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupName))
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))
		})

		Step("Create schedule1 with user1 and verify the schedule1 and backups are not accessible to user2", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule with user1
			schedule1Name := fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(schedule1Name, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", schedule1Name))

			// verify the backup schedule is not accessible to user2
			schedules, err := GetAllBackupSchedulesForUser(testUser2, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all schedules for user")
			dash.VerifyFatal(len(schedules), 0, "Verifying no schedules are accessible to other shared user")

			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			backupScheduleInspectreq := &api.BackupScheduleInspectRequest{
				OrgId: BackupOrgID,
				Name:  schedule1Name,
			}
			_, err = Inst().Backup.InspectBackupSchedule(testUser2Ctx, backupScheduleInspectreq)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("verifying schedule [%s] created by [%s] is not accessible to [%s]", schedule1Name, testUser1, testUser2))

			// verify the backups are not accessible to user2
			backups, err := GetAllBackupsForUser(testUser2, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all backups for user")
			dash.VerifyFatal(len(backups), 0, "Verifying no backups are accessible to other shared user")
		})

		Step("Create schedule2 with user2 and verify the schedule2 and backups are not accessible to user1", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule with user2
			schedule2Name := fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(schedule2Name, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", schedule2Name))

			// verify the backup schedule is not accessible to user1
			schedules, err := GetAllBackupSchedulesForUser(testUser1, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all schedules for user")
			dash.VerifyFatal(len(schedules), 2, "Verifying only owned schedules are accessible to user")

			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			backupScheduleInspectreq := &api.BackupScheduleInspectRequest{
				OrgId: BackupOrgID,
				Name:  schedule2Name,
			}
			_, err = Inst().Backup.InspectBackupSchedule(testUser1Ctx, backupScheduleInspectreq)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("verifying schedule [%s] created by [%s] is not accessible to [%s]", schedule2Name, testUser2, testUser1))

			// verify the backups are not accessible to user1
			backups, err := GetAllBackupsForUser(testUser1, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all backups for user")
			dash.VerifyFatal(len(backups), 2, "Verifying only owned backups are accessible to user")
		})
	})

	// This test case verifies the cluster share operation from app user to any user with backups, schedules and restore validations
	It("Verify cluster share from one appuser to any user with backups, schedules and restore creation", func() {
		StartPxBackupTorpedoTest("VerifyClusterSharefromAppUserToAnyUSer", "Verify Cluster share from app user to any user with backups, schedules ad restores", nil, 301029, Dbinnal, Q2FY25)

		var testUser3 = fmt.Sprintf("testuser-%s", RandomString(5))
		Step("Create a cluster object with testuser1 and Share with testuser2", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			err = AddSourceCluster(testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, appAdminUser))
			log.InfoD("Cluster [%s] created successfully", SourceClusterName)

			clusterUid, err = Inst().Backup.GetClusterUID(testUser1Ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
			backupsList = append(backupsList, backupName)

			// Share the source cluster to testuser1
			_, err = ShareCluster(testUser1Ctx, SourceClusterName, clusterUid, []string{testUser2}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s users using %s user", SourceClusterName, []string{testUser2}, appAdminUser))

			// Verify backups/restores/schedules are not accessible to shared user
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(testUser2Ctx, testUser1, CommonPassword, clusterUid)
		})

		Step("Verify backup creation with testuser2 on shared cluster", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
			log.Infof("Backup [%s] created successfully by user: [%s]", backupName, testUser2)

			// Delete backup
			backupUID, err := Inst().Backup.GetBackupUID(testUser2Ctx, backupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupName))
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))
		})

		Step("Create a backup schedule with testuser2 on shared cluster", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule
			scheduleName := fmt.Sprintf("schedule-bkp-%v", RandomString(5))

			err = CreateScheduleBackup(scheduleName, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", scheduleName))
		})

		Step("Create backup and restore it on the same cluster(Shared Cluster) with testuser2 user", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
			log.Infof("Backup [%s] created successfully by user: [%s]", backupName, testUser2)

			restoreName := fmt.Sprintf("%s-%v", RestoreNamePrefix, time.Now().Unix())
			namespaceMapping := make(map[string]string)

			for _, namespace := range bkpNamespaces {
				restoredNameSpace := fmt.Sprintf("%s-%s", namespace, "restored")
				namespaceMapping[namespace] = restoredNameSpace
			}
			// Create Restore
			err = CreateRestore(restoreName, backupName, namespaceMapping, SourceClusterName, clusterUid, BackupOrgID, testUser2Ctx, make(map[string]string))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of restore [%s]", restoreName))
			restoreList = append(restoreList, restoreName)

			// Delete backup
			backupUID, err := Inst().Backup.GetBackupUID(testUser2Ctx, backupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupName))
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))
		})

		Step("Add testUser3 and share cluster, backupLocation and schedule policy with testUser3", func() {
			// Creating test user
			firstName := fmt.Sprintf("FirstName-%s", testUser3)
			lastName := fmt.Sprintf("LastName-%s", testUser3)
			email := fmt.Sprintf("%v@cnbu.com", testUser3)
			err := backup.AddUser(testUser3, firstName, lastName, email, CommonPassword)
			log.FailOnError(err, "Failed to create user - %s", testUser3)

			adminCtx, err := backup.GetAdminCtxFromSecret()
			log.FailOnError(err, "Fetching admin user ctx")
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Share the source cluster to testuser3
			_, err = ShareCluster(testUser1Ctx, SourceClusterName, clusterUid, []string{testUser3}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user", SourceClusterName, testUser3))

			// Share the backup location to testuser3
			err = AddBackupLocationOwnership(backupLocationName, backupLocationUID, []string{testUser3}, nil, Read, Invalid, adminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of backup location [%s] with %s user", backupLocationName, testUser3))

			// Share the schedule policy to testuser3
			err = AddSchedulePolicyOwnership(periodicSchedulePolicyName, periodicSchedulePolicyUid, []string{testUser3}, nil, Read, Invalid, adminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of schedule policy [%s] with %s user", periodicSchedulePolicyName, testUser3))
		})

		Step("Create schedule1 with testuser2 and verify the schedule1 and backups are not accessible to testUser3", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule with user1
			schedule1Name := fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(schedule1Name, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser2Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", schedule1Name))

			// verify the backup schedule is not accessible to testUser3
			schedules, err := GetAllBackupSchedulesForUser(testUser3, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all schedules for user")
			dash.VerifyFatal(len(schedules), 0, "Verifying no schedules are accessible to other shared user")

			testUser3Ctx, err := backup.GetNonAdminCtx(testUser3, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			backupScheduleInspectreq := &api.BackupScheduleInspectRequest{
				OrgId: BackupOrgID,
				Name:  schedule1Name,
			}
			_, err = Inst().Backup.InspectBackupSchedule(testUser3Ctx, backupScheduleInspectreq)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("verifying schedule [%s] created by [%s] is not accessible to [%s]", schedule1Name, testUser2, testUser3))

			// verify the backups are not accessible to user2
			backups, err := GetAllBackupsForUser(testUser3, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all backups for user")
			dash.VerifyFatal(len(backups), 0, "Verifying no backups are accessible to other shared user")
		})

		Step("Create schedule2 with testUser3 and verify the schedule2 and backups are not accessible to testUser2", func() {
			testUser3Ctx, err := backup.GetNonAdminCtx(testUser3, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Create a backup schedule with user2
			schedule2Name := fmt.Sprintf("schedule-bkp-%v", RandomString(5))
			err = CreateScheduleBackup(schedule2Name, SourceClusterName, clusterUid, backupLocationName, backupLocationUID, bkpNamespaces, make(map[string]string), BackupOrgID, "", "", "", "", periodicSchedulePolicyName, periodicSchedulePolicyUid, testUser3Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of scheduled backup with schedule name [%s]", schedule2Name))

			// verify the backup schedule is not accessible to user1
			schedules, err := GetAllBackupSchedulesForUser(testUser2, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all schedules for user")
			dash.VerifyFatal(len(schedules), 2, "Verifying only owned schedules are accessible to user")

			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			backupScheduleInspectreq := &api.BackupScheduleInspectRequest{
				OrgId: BackupOrgID,
				Name:  schedule2Name,
			}
			_, err = Inst().Backup.InspectBackupSchedule(testUser2Ctx, backupScheduleInspectreq)
			dash.VerifyNotNilFatal(err, fmt.Sprintf("verifying schedule [%s] created by [%s] is not accessible to [%s]", schedule2Name, testUser3, testUser2))

			// verify the backups are not accessible to user1
			backups, err := GetAllBackupsForUser(testUser2, CommonPassword)
			dash.VerifyFatal(err, nil, "Failed to get all backups for user")
			dash.VerifyFatal(len(backups), 2, "Verifying only owned backups are accessible to user")
		})
	})

	// This test case verifies the cluster share operation from app user to any user with backups, schedules and restore validations
	It("As a cluster shared user verify backup share is allowed", func() {
		StartPxBackupTorpedoTest("AsClusterSharedUserVerifyBackupShareIsAllowed", "As a cluster shared user verify backup share is allowed", nil, 301031, Dbinnal, Q2FY25)

		sharedBackupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())

		Step("Create a cluster object with admin and Share with testuser1", func() {
			adminCtx, err := backup.GetAdminCtxFromSecret()
			log.FailOnError(err, "Fetching admin user ctx")

			err = AddSourceCluster(adminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, appAdminUser))

			clusterUid, err = Inst().Backup.GetClusterUID(adminCtx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))

			// Create a backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", adminCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
			backupsList = append(backupsList, backupName)

			// Share the source cluster to testuser1
			_, err = ShareCluster(adminCtx, SourceClusterName, clusterUid, []string{testUser1}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s users using %s user", SourceClusterName, []string{testUser1}, appAdminUser))

			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")
			VerifySourceClusterAccessWithHavingNoBackupsRestoresANDBackupScheduleObjects(testUser1Ctx, testUser1, CommonPassword, clusterUid)
		})

		Step("Create a backup with testUser1 and verify backup share is allowed to testuser2", func() {
			testUser1Ctx, err := backup.GetNonAdminCtx(testUser1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(sharedBackupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", sharedBackupName))

			err = ShareBackup(sharedBackupName, nil, []string{testUser2}, ViewOnlyAccess, testUser1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of backup [%s] with %s users using %s user", sharedBackupName, []string{testUser2}, testUser1))
		})

		Step("Verify user2 can access the shared backup", func() {
			testUser2Ctx, err := backup.GetNonAdminCtx(testUser2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			backupUId, err := Inst().Backup.GetBackupUID(testUser2Ctx, sharedBackupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupsList[0]))

			backupInspectReq := &api.BackupInspectRequest{
				OrgId: BackupOrgID,
				Name:  sharedBackupName,
				Uid:   backupUId,
			}
			_, err = Inst().Backup.InspectBackup(testUser2Ctx, backupInspectReq)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying backup [%s] is accessible to shared user", sharedBackupName))
		})

		Step("unshare cluster from testuser1", func() {
			adminCtx, err := backup.GetAdminCtxFromSecret()
			log.FailOnError(err, "Fetching admin user ctx")

			// Un-Share the source cluster with testuser1
			_, err = UnShareCluster(adminCtx, SourceClusterName, clusterUid, []string{testUser1}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s users using %s ctx", SourceClusterName, []string{testUser1}, appAdminUser))
		})
	})

	AfterEach(func() {
		// Cleanup
		defer EndPxBackupTorpedoTest(scheduledAppContexts)
		opts := make(map[string]bool)
		opts[SkipClusterScopedObjects] = true
		ctx, err := backup.GetPxCentralAdminCtx()
		log.FailOnError(err, "Fetching admin user ctx")

		enumeratereq := &api.BackupScheduleEnumerateRequest{
			OrgId: BackupOrgID,
		}
		schedules, err := Inst().Backup.EnumerateBackupSchedule(ctx, enumeratereq)
		log.FailOnError(err, "Failed to enumerate backup schedules")
		for _, schedule := range schedules.GetBackupSchedules() {
			err = DeleteScheduleWithUIDAndWait(schedule.GetName(), schedule.GetUid(), SourceClusterName, clusterUid, BackupOrgID, ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying deletion of backup schedule [%s]", schedule.GetName()))
		}

		// Delete the schedule policy
		if len(periodicSchedulePolicyName) > 0 {
			log.InfoD("Deleting schedule policy [%s]", periodicSchedulePolicyName)
			schedulePolicyDeleteRequest := &api.SchedulePolicyDeleteRequest{
				Name:  periodicSchedulePolicyName,
				Uid:   periodicSchedulePolicyUid,
				OrgId: BackupOrgID,
			}
			_, err := Inst().Backup.DeleteSchedulePolicy(ctx, schedulePolicyDeleteRequest)
			log.FailOnError(err, "failed to delete schedule policy [%s]", periodicSchedulePolicyName)
		}

		enumerateBackupReq := &api.BackupEnumerateRequest{
			OrgId: BackupOrgID,
		}
		backups, err := Inst().Backup.EnumerateBackup(ctx, enumerateBackupReq)
		log.FailOnError(err, "Failed to enumerate backups")
		// Delete the backups
		for _, backup := range backups.GetBackups() {
			backupUid, err := Inst().Backup.GetBackupUID(ctx, backup.Name, BackupOrgID)
			log.FailOnError(err, "Unable to fetch backup UID")
			err = DeleteBackupAndWaitForCompletion(backup.Name, backupUid, BackupOrgID, ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backup))
		}

		// Delete the restores
		for _, restore := range restoreList {
			err = DeleteRestore(restore, BackupOrgID, ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting restore [%s]", restore))
		}

		log.InfoD("Deleting deployed applications")
		DestroyApps(scheduledAppContexts, opts)

		err = DeleteNamespaces(bkpNamespaces)
		log.FailOnError(err, "failed to delete namespaces")

		CleanupCloudSettingsAndClusters(backupLocationMap, cloudCredName, cloudCredUID, ctx)

		// Delete the users
		for _, user := range []string{appAdminUser, testUser1, testUser2} {
			err = backup.DeleteUser(user)
			log.FailOnError(err, "Failed to delete user - %s", user)
		}
	})
})

var _ = Describe("{ClusterShareWithSuperAdminOperations}", Label(TestCaseLabelsMap[ClusterShare]...), func() {
	var (
		superAdmin1          string
		superAdmin2          string
		testUser             string
		clusterUid           string
		superAdminRole       backup.PxBackupRole = backup.SuperAdmin
		backupsList          []string
		backupLocationName   string
		backupLocationUID    string
		cloudCredName        string
		cloudCredUID         string
		backupLocationMap    map[string]string
		scheduledAppContexts []*scheduler.Context
		bkpNamespaces        []string
	)

	BeforeEach(func() {
		backupLocationMap = make(map[string]string)
		scheduledAppContexts = make([]*scheduler.Context, 0)
		backupsList = make([]string, 0)
		bkpNamespaces = make([]string, 0)
		bkpNamespaces = make([]string, 0)
		superAdmin1 = CreateUsers(1)[0]
		superAdmin2 = CreateUsers(1)[0]
		testUser = CreateUsers(1)[0]

		// Schedule an Application
		appContexts := ScheduleApplications(TaskNamePrefix)
		for _, ctx := range appContexts {
			ctx.ReadinessTimeout = AppReadinessTimeout
			namespace := GetAppNamespace(ctx, TaskNamePrefix)
			bkpNamespaces = append(bkpNamespaces, namespace)
			scheduledAppContexts = append(scheduledAppContexts, ctx)
		}
		// Validate the application
		ValidateApplications(scheduledAppContexts)

		for _, user := range []string{superAdmin1, superAdmin2} {
			err := backup.AddRoleToUser(user, superAdminRole, fmt.Sprintf("Adding %v role to %s", superAdminRole, user))
			log.FailOnError(err, "failed to add role %s to the user %s", superAdminRole, user)
		}

		// Create Backup Location and Cloud Setting
		adminContext, err := backup.GetAdminCtxFromSecret()
		log.FailOnError(err, "Fetching admin user ctx")
		log.InfoD("Creating backup location and cloud setting with admin user")
		backupLocationProviders := GetBackupProviders()
		for _, provider := range backupLocationProviders {
			cloudCredName = fmt.Sprintf("%s-cred-%v", provider, RandomString(10))
			cloudCredUID = uuid.New()
			err := CreateCloudCredential(provider, cloudCredName, cloudCredUID, BackupOrgID, adminContext)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of cloud credential named [%s] for org [%s]  as provider %s", cloudCredName, BackupOrgID, provider))

			backupLocationName = fmt.Sprintf("%s-backup-location-%v", provider, RandomString(10))
			backupLocationUID = uuid.New()
			backupLocationMap[backupLocationUID] = backupLocationName
			err = CreateBackupLocationWithContext(provider, backupLocationName, backupLocationUID, cloudCredName, cloudCredUID, getGlobalBucketName(provider), BackupOrgID, "", adminContext, true)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creating backup location %s", backupLocationName))
		}

		// Share backup location with app user
		err = AddBackupLocationOwnership(backupLocationName, backupLocationUID, []string{testUser}, nil, Read, Invalid, adminContext)
		dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of backup location [%s] with users [%v] using admin ctx", backupLocationName, []string{testUser}))
	})

	It("As a superadmin share cluster owner by other superadmin", func() {
		StartPxBackupTorpedoTest("AsSuperAdminShareClusterOwedbyOtherSuperAdmin", "As a superadmin share cluster owner by other superadmi", nil, 301030, Dbinnal, Q2FY25)

		Step("Create a cluster object with superadmin1", func() {
			superAdmin1Ctx, err := backup.GetNonAdminCtx(superAdmin1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			err = AddSourceCluster(superAdmin1Ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of source [%s] cluster with %s ctx", SourceClusterName, superAdmin1))
			log.InfoD("Cluster [%s] created successfully", SourceClusterName)

			clusterUid, err = Inst().Backup.GetClusterUID(superAdmin1Ctx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		Step("Share the cluster with testUser from superAdmin2", func() {
			superAdmin2Ctx, err := backup.GetNonAdminCtx(superAdmin2, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			_, err = ShareCluster(superAdmin2Ctx, SourceClusterName, clusterUid, []string{testUser}, nil, false)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user", SourceClusterName, testUser))
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying share of source [%s] cluster with %s user", SourceClusterName, testUser))
		})

		Step("Verify backup creation from testUser", func() {
			testUserCtx, err := backup.GetNonAdminCtx(testUser, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			// Take Backup
			backupName := fmt.Sprintf("%s-%v", BackupNamePrefix, time.Now().Unix())
			log.InfoD(fmt.Sprintf("Taking backup of multiple namespaces [%v]", bkpNamespaces))
			err = CreateBackup(backupName, SourceClusterName, backupLocationName, backupLocationUID, bkpNamespaces, nil, BackupOrgID, clusterUid, "", "", "", "", testUserCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))

			// Delete backup
			backupUID, err := Inst().Backup.GetBackupUID(testUserCtx, backupName, BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] backup uid", backupName))
			err = DeleteBackupAndWaitForCompletion(backupName, backupUID, BackupOrgID, testUserCtx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backupName))
		})

		Step("Unshare cluster with testUser from superAdmin1", func() {
			superAdmin1Ctx, err := backup.GetNonAdminCtx(superAdmin1, CommonPassword)
			log.FailOnError(err, "Fetching non admin ctx")

			_, err = UnShareCluster(superAdmin1Ctx, SourceClusterName, clusterUid, []string{testUser}, nil)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying Unshare of source [%s] cluster with %s user", SourceClusterName, testUser))
		})
	})

	AfterEach(func() {
		// Cleanup
		defer EndPxBackupTorpedoTest(scheduledAppContexts)
		opts := make(map[string]bool)
		opts[SkipClusterScopedObjects] = true
		ctx, err := backup.GetPxCentralAdminCtx()
		log.FailOnError(err, "Fetching admin user ctx")

		// Delete the backups
		for _, backup := range backupsList {
			backupUid, err := Inst().Backup.GetBackupUID(ctx, backup, BackupOrgID)
			log.FailOnError(err, "Unable to fetch backup UID")
			err = DeleteBackupAndWaitForCompletion(backup, backupUid, BackupOrgID, ctx)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Deleting backup [%s]", backup))
		}

		err = DeleteNamespaces(bkpNamespaces)
		log.FailOnError(err, "failed to delete namespaces")

		CleanupCloudSettingsAndClusters(backupLocationMap, cloudCredName, cloudCredUID, ctx)

		// Delete the users
		for _, user := range []string{superAdmin1, superAdmin2, testUser} {
			err = backup.DeleteUser(user)
			log.FailOnError(err, "Failed to delete user - %s", user)
		}
	})
})

// This test case verifies if Backup can be shared at cluster level to a group from a normal user with full access and then deleted from admin user.
var _ = Describe("{BackupShareClusterToGroupFromNormalUserLaterDeleteFromAdmin}", Label(TestCaseLabelsMap[ClusterShare]...), func() {
	var (
		backupName           string
		scheduledAppContexts []*scheduler.Context
		sourceClusterUid     string
		clusterStatus        api.ClusterInfo_StatusInfo_Status
		cloudCredName        string
		cloudCredUID         string
		backupLocationUID    string
		backupLocation       string
		group1               string
		secondUserName       string
		firstUserName        string
		bkpNamespaces        []string
		allUsers             []string
	)
	bkpNamespaces = make([]string, 0)
	labelSelectors := make(map[string]string)
	backupLocationMap := make(map[string]string)
	backupNames := make([]string, 0)
	allUsers = make([]string, 0)
	numOfBackup := 2

	JustBeforeEach(func() {
		StartPxBackupTorpedoTest("VerifyBackupShareClusterToGroupFromNormalUserLaterDeleteFromAdmin",
			"Share Backup Cluster to Group from Normal User and Delete From Admin", nil, 300838, ABadgujar, Q2FY24)
		//1.Step-Deploy Applications in the Cluster
		log.InfoD("Deploy applications")
		scheduledAppContexts = make([]*scheduler.Context, 0)
		for i := 0; i < Inst().GlobalScaleFactor; i++ {
			taskName := fmt.Sprintf("%s-%d", TaskNamePrefix, i)
			appContexts := ScheduleApplications(taskName)
			for _, ctx := range appContexts {
				ctx.ReadinessTimeout = AppReadinessTimeout
				namespace := GetAppNamespace(ctx, taskName)
				bkpNamespaces = append(bkpNamespaces, namespace)
				scheduledAppContexts = append(scheduledAppContexts, ctx)
			}
		}
	})
	It("Share Backup Cluster to Group from Normal User and Delete From Admin", func() {
		//2.Step-Validate Deployed Applications
		Step("Validate applications", func() {
			log.InfoD("Validating apps")
			ValidateApplications(scheduledAppContexts)
		})

		//3.Step-Create 2 Users with Infra Admin Role
		Step("Create 2 Users with Infra Admin Role", func() {
			log.InfoD("Create 2 Users with Infra Admin Role")
			role := backup.InfrastructureOwner
			allUsers = CreateUsers(2)
			for _, userName := range allUsers {
				err := backup.AddRoleToUser(userName, role, fmt.Sprintf("Adding %v role to %s", role, userName))
				log.FailOnError(err, "Failed to add role for user - %s", userName)
				//Append both usernames to Slice of Usernames and store them for future use
				log.FailOnError(err, "Failed to fetch uid for - %s", userName)
			}
			firstUserName = allUsers[0]
			secondUserName = allUsers[1]
		})

		//4.Step-Create a group with full access
		Step("Create Groups", func() {
			log.InfoD("Creating 1 user group for full access")
			group1 = "group-with-full-access"
			err := backup.AddGroup(group1)
			log.FailOnError(err, "Failed to create user group-1- %v", group1)
		})

		//5.Step-Add users to the group1
		Step("Add pxb users to group of pxb-users", func() {
			log.InfoD("Adding users to group1")
			err := backup.AddGroupToUser(secondUserName, group1)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Adding user %s to group %s", secondUserName, group1))

			usersOfGroup, err := backup.GetMembersOfGroup(group1)
			log.FailOnError(err, "Error fetching members of the group - %v", group1)
			log.Infof("Group [%v] contains the following users: \n%v", group1, usersOfGroup)
		})

		//6.Step-Creating Cloud Credentials with User1 context and Backup Location
		Step("Creating backup location and cloud setting", func() {
			log.InfoD("Creating backup location and cloud setting")
			nonAdminCtx, err := backup.GetNonAdminCtx(firstUserName, CommonPassword)
			log.FailOnError(err, "Fetching User 1 context")
			providers := GetBackupProviders()
			for _, provider := range providers {
				cloudCredName = fmt.Sprintf("%s-%s-%v", "cloudcred", provider, time.Now().Unix())
				backupLocation = fmt.Sprintf("%v-bl", time.Now().Unix())
				cloudCredUID = uuid.New()
				backupLocationUID = uuid.New()
				backupLocationMap[backupLocationUID] = backupLocation
				err := CreateCloudCredential(provider, cloudCredName, cloudCredUID, BackupOrgID, nonAdminCtx)
				dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying creation of cloud credential named [%s] for org [%s] with [%s] as provider", cloudCredName, BackupOrgID, provider))
				err = CreateBackupLocationWithContext(provider, backupLocation, backupLocationUID, cloudCredName, cloudCredUID, getGlobalBucketName(provider), BackupOrgID, "", nonAdminCtx, true)
				dash.VerifyFatal(err, nil, fmt.Sprintf("Creating backup location %s", backupLocation))
			}
		})

		//7.Step-Registering Cluster for Backup
		Step("Register cluster for backup", func() {
			nonAdminCtx, err := backup.GetNonAdminCtx(firstUserName, CommonPassword)
			log.FailOnError(err, "Fetching user context")
			err = CreateApplicationClusters(BackupOrgID, "", "", nonAdminCtx)
			dash.VerifyFatal(err, nil, "Creating source and destination cluster")
			clusterStatus, err = Inst().Backup.GetClusterStatus(BackupOrgID, SourceClusterName, nonAdminCtx)
			log.FailOnError(err, fmt.Sprintf("Fetching [%s] cluster status", SourceClusterName))
			dash.VerifyFatal(clusterStatus, api.ClusterInfo_StatusInfo_Online, fmt.Sprintf("Verifying if [%s] cluster is online", SourceClusterName))
			sourceClusterUid, err = Inst().Backup.GetClusterUID(nonAdminCtx, BackupOrgID, SourceClusterName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching [%s] cluster uid", SourceClusterName))
		})

		//8.Step-Create 2 Backups in User1 Context
		Step("Taking backup of applications", func() {
			nonAdminCtx, err := backup.GetNonAdminCtx(firstUserName, CommonPassword)
			log.FailOnError(err, "Fetching user [%s] ctx", firstUserName)
			for i := 0; i < numOfBackup; i++ {
				backupName = fmt.Sprintf("%s-%s-%v", BackupNamePrefix, bkpNamespaces[0], time.Now().Unix())
				appContextsToBackup := FilterAppContextsByNamespace(scheduledAppContexts, bkpNamespaces)
				err = CreateBackupWithValidation(nonAdminCtx, backupName, SourceClusterName, backupLocation, backupLocationUID, appContextsToBackup, labelSelectors, BackupOrgID, sourceClusterUid, "", "", "", "")
				dash.VerifyFatal(err, nil, fmt.Sprintf("Creation of backup [%s]", backupName))
				backupNames = append(backupNames, backupName)
			}
		})

		//9.Step-Share backup with Group1 containing User2 having full access
		Step("Share backup with Group1 containing User2 having full access", func() {
			log.InfoD("Share all backups with Full Access in source cluster with a group")
			nonAdminCtx, err := backup.GetNonAdminCtx(firstUserName, CommonPassword)
			log.FailOnError(err, "Fetching User Context")
			err = ClusterUpdateBackupShare(SourceClusterName, []string{group1}, nil, FullAccess, true, nonAdminCtx)
			log.FailOnError(err, "Failed sharing all backups for cluster [%s]", SourceClusterName)
		})

		//10.Step-Enumerating Backup for 2nd User
		Step("Enumerating Backup for 2nd User", func() {
			nonAdminCtx, err := backup.GetNonAdminCtx(secondUserName, CommonPassword)
			log.FailOnError(err, "Fetching user [%s] ctx", secondUserName)
			resp, err := Inst().Backup.EnumerateBackup(nonAdminCtx, &api.BackupEnumerateRequest{
				OrgId: BackupOrgID,
			})
			dash.VerifyFatal(err, nil, "Enumerating backups")
			dash.VerifyFatal(len(resp.GetBackups()), 2, fmt.Sprintf("Enumerating backups using [%s] ctx", secondUserName))
		})

		//11.Step-Delete backup from Admin Context
		Step("Delete Backup from Admin User", func() {
			ctx, err := backup.GetAdminCtxFromSecret()
			log.FailOnError(err, "Fetching px-central-admin ctx")
			backupUID, err := Inst().Backup.GetBackupUID(ctx, backupNames[0], BackupOrgID)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Fetching the backup %s uid", backupNames[0]))
			_, err = DeleteBackup(backupNames[0], backupUID, BackupOrgID, ctx)
			tBackupName := backupNames[0]
			log.FailOnError(err, "Backup [%v] could not be deleted by user admin user", tBackupName)
			dash.VerifyFatal(err, nil, fmt.Sprintf("Verifying backup %s deletion", tBackupName))
		})
	})

	JustAfterEach(func() {
		defer EndPxBackupTorpedoTest(scheduledAppContexts)
		log.InfoD("Deleting the deployed apps after the testcase")
		// Cleaning up applications created
		opts := make(map[string]bool)
		opts[SkipClusterScopedObjects] = true
		DestroyApps(scheduledAppContexts, opts)

		// Clean the all user created
		var wg sync.WaitGroup
		log.Infof("Cleaning up users")
		for _, userName := range allUsers {
			wg.Add(1)
			go func(userName string) {
				defer GinkgoRecover()
				defer wg.Done()
				err := backup.DeleteUser(userName)
				log.FailOnError(err, "Error deleting user %v", userName)
			}(userName)
		}
		wg.Wait()

		// Clean up the all group created
		log.Infof("Cleaning up groups")
		err := backup.DeleteGroup(group1)
		log.FailOnError(err, "Error deleting user %v", group1)

		// Clean up the cluster
		ctx, err := backup.GetAdminCtxFromSecret()
		log.FailOnError(err, "Fetching px-central-admin ctx")
		CleanupCloudSettingsAndClusters(backupLocationMap, cloudCredName, cloudCredUID, ctx)

	})
})
